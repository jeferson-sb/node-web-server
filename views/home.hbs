<!DOCTYPE html>
<html lang="en" dir="ltr">
  {{>head}}
<body>
  <section class="section">
    <div class="container">
      {{>header}}
      <p>{{screamIt welcomeMessage}}</p>
    </div>
  </section>
 {{>footer}}
</body>
</html>